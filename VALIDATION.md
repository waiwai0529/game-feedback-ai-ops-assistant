# 验证记录

验证日期：2026-09-14。环境：Windows，Python 3.13.9。

- `python -m unittest -v`：14 项测试通过。
- `python main.py demo`：完整生成 JSON、5 类 CSV 输出和 Markdown 报告。
- 12 条手写演示样例汇总为 12 条成功、10 条负向、5 条待复核；这些数字仅说明样例的汇总情况，不是模型指标。
- 手工核对优先级：演示中的存档问题 1 × 3 × 3 = 9；两条帧率问题 2 × (2 × 2) = 8，与导出一致。
- 自动测试覆盖：伪造证据、布尔值伪装分数、非负向评分矛盾、重复 ID、CSV 公式转义、错误 JSON 修复、断点续跑与配置变化、Steam 游标编码和去重、Apple RSS 采集与身份字段排除、开发/盲标集隔离、零成功结果、人工评估覆盖率与缺失标签。
- 《蛋仔派对》第一天数据检查：近期数据集 500 条、500 个唯一 ID，其中 208 条为非 5 星；已拆出 30 条开发样本和 50 条盲标样本且无重叠。第一轮 30 条人工观察中的 topic 均在代码枚举中。
- `day2.ps1` 已通过 PowerShell 语法解析；真实 API Key、付费账户和线上模型调用仍需用户执行。新增 QA 抽样测试，确认预测字段与人工复核字段正确分离。

未完成或不在本次验证范围：真实付费大模型调用、线上 Steam 采集、人工盲标准确率、Excel 原生工作簿操作。API 和 Steam 行为使用模拟响应测试。Dashboard.xlsx 需按 README 在 Excel 中导入数据并制作。本包未包含已完成的 Excel Dashboard。
